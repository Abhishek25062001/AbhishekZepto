import { emptyObjectValidator } from './common.validators';

export const healthEndpointValidator = {
  query: emptyObjectValidator,
};

export const versionEndpointValidator = {
  query: emptyObjectValidator,
};

export const systemInfoEndpointValidator = {
  query: emptyObjectValidator,
};

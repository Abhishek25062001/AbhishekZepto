# Web Theme Usage

## Goal

Vendor Panel and Admin Dashboard components must use shared theme CSS variables or local theme tokens that match the design token foundation.

## Component Rule

Web components must use theme CSS variables or theme tokens.

## Dashboard Layout Rule

Dashboard layouts must not use random hardcoded brand colors. Use the web theme variables for brand, semantic, neutral, spacing, and radius values.

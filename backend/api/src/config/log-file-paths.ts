export const LOG_DIR = 'logs';
export const ERROR_LOG_FILE = 'error.log';
export const COMBINED_LOG_FILE = 'combined.log';

// Phase 1 uses console logging. File transport can be added later if needed.

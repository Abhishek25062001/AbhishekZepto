import { Router } from 'express';

/**
 * Admin catalog product list/search is integrated on
 * `product-admin.routes.ts` (`GET /`) to avoid duplicate route registration.
 */
const router = Router();

export default router;

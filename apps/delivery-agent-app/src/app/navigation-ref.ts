import { createNavigationContainerRef } from '@react-navigation/native';

import type { MainStackParamList } from './navigation.types';

export const deliveryNavigationRef = createNavigationContainerRef<MainStackParamList>();
